#define NOT_ECHO 0
  /* getch(), gethex() 呼び出し時に受信文字をエコーしない */
#define DO_ECHO 1
  /* getch(), gethex() 呼び出し時に受信文字をエコーする */
#define NOTSENDCR 0
  /* putstr() 呼び出し時に最後に受信文字を付加しない */
#define SENDCR 1
  /* putstr() 呼び出し時に最後に受信文字を付加する */
